import { Button, Input } from '@chakra-ui/react'
import React from 'react'

const JoinNCreateBooky = () => {
  return (
    <div>
      {/* {isShown &&
              <div
                className='flex flex-col gap-2' >
                <h1 className='text-2xl dark:text-white'>Edit profile</h1>
                <Input className='dark:text-white' placeholder='New username' ref={newNameRef} />
                <Input className='dark:text-white' placeholder='New profile picture url' ref={newPhotoRef} />
                <Button onClick={updateProfile} colorScheme='blue'>Join a booky</Button>
                <Button onClick={() => setIsShown(false)} colorScheme='red'>Create a booky</Button>
              </div>} */}
    </div>
  )
}

export default JoinNCreateBooky